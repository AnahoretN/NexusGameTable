# 🎯 Примеры размещения пул-панелей

## 📐 Зоны поиска территорий

### Видимая зона (запрещена для пул-панелей)
```
┌─────────────────────────────┐
│          5000×5000          │  ← Игроки видят только эту зону
│   PLAYABLE AREA (0-5000)    │  ← Скроллбары показывают только это
│                             │
│    ┌───────────────────┐    │
│    │   Основной стол    │    │
│    │   для игроков      │    │
│    └───────────────────┘    │
└─────────────────────────────┘
```

### Зона для пул-панелей (справа)
```
      5000      6000      7000      8000      9000     10000
       │         │         │         │         │         │
       ▼         ▼         ▼         ▼         ▼         ▼
┌─────┬─────────┬─────────┬─────────┬─────────┬─────────┬─────┐
│  Y= │   0     │  1000   │  2000   │  3000   │  4000   │ ... │
├─────┼─────────┼─────────┼─────────┼─────────┼─────────┼─────┤
│  0  │ Pool #1 │ Pool #2 │ Pool #3 │ Pool #4 │ Pool #5 │ ... │  ← Первая линия
├─────┼─────────┼─────────┼─────────┼─────────┼─────────┼─────┤
│1000 │ Pool #6 │ Pool #7 │ Pool #8 │ Pool #9 │ Pool #10│ ... │  ← Вторая линия
├─────┼─────────┼─────────┼─────────┼─────────┼─────────┼─────┤
│2000 │ Pool #11│ Pool #12│ Pool #13│ Pool #14│ Pool #15│ ... │  ← Третья линия
└─────┴─────────┴─────────┴─────────┴─────────┴─────────┴─────┘
```

### Зона для пул-панелей (снизу)
```
        0        1000      2000      3000      4000      5000
        │         │         │         │         │         │
        ▼         ▼         ▼         ▼         ▼         ▼
┌───────┬─────────┬─────────┬─────────┬─────────┬─────────┬───────┐
│ X=5000│  Pool # │  Pool # │  Pool # │  Pool # │  Pool # │  ...  │  ← Снизу
└───────┴─────────┴─────────┴─────────┴─────────┴─────────┴───────┘
```

## 🔍 Алгоритм поиска

### Шаг 1: Поиск справа (приоритет)
```typescript
// Стратегия 1: x >= 5000, все Y от 0 до 10000
for (let y = 0; y <= 9000; y += 1000) {
  for (let x = 5000; x <= 9000; x += 1000) {
    // Проверяем что территория НЕ пересекается с playable area
    // и НЕ пересекается с существующими пул-панелями
  }
}
```

### Шаг 2: Поиск снизу (если справа нет места)
```typescript
// Стратегия 2: y >= 5000, x от 0 до 5000
for (let y = 5000; y <= 9000; y += 1000) {
  for (let x = 0; x < 5000; x += 1000) {
    // Те же проверки
  }
}
```

## 📋 Примеры координат

### Первые 5 пул-панелей (справа)
```
Pool 1: (5000, 0)    ← Справа вверху
Pool 2: (6000, 0)    ← Справа от первой
Pool 3: (7000, 0)    ← Справа от второй
Pool 4: (8000, 0)    ← Справа от третьей
Pool 5: (9000, 0)    ← Справа от четвёртой
```

### Следующие 5 пул-панелей (вторая линия)
```
Pool 6:  (5000, 1000) ← Под первой
Pool 7:  (6000, 1000) ← Под второй
Pool 8:  (7000, 1000) ← Под третьей
Pool 9:  (8000, 1000) ← Под четвёртой
Pool 10: (9000, 1000) ← Под пятой
```

### Если справа заполнено (снизу)
```
Pool 51: (0, 5000)    ← Снизу слева
Pool 52: (1000, 5000) ← Снизу от первой
Pool 53: (2000, 5000) ← Снизу от второй
...
```

## ⚠️ Запрещённые координаты

Эти координаты НЕ будут использоваться:
```
❌ (0, 0)     → Внутри playable area
❌ (1000, 0)  → Внутри playable area
❌ (4000, 0)  → Внутри playable area
❌ (4500, 0)  → Пересекается с playable area
❌ (0, 5000)  → Граница playable area (используется только если справа нет места)
```

## 🎮 Результат для игрока

- **Скроллбары**: показывают только 0-5000×0-5000
- **Видимая зона**: игрок видит только основной стол
- **Пул-панели**: находятся "в другом измерении"
- **Пользовательский опыт**: пул-панели работают как отдельные пространства

## 🔧 Проверка в коде

```typescript
// Проверка что территория вне playable area
if (isInPlayableArea(territory)) {
  return {
    valid: false,
    error: `Pool territory overlaps playable area`
  };
}
```

Логика гарантирует, что пул-панели **НИКОГДА** не будут созданы в видимой зоне!