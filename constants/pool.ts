/**
 * Pool panel constants
 */

/**
 * Default pool dimensions in virtual units
 */
export const DEFAULT_POOL_WIDTH = 1000;
export const DEFAULT_POOL_HEIGHT = 1000;

/**
 * Drag threshold for adding cards/tokens to cursor slot (in pixels)
 */
export const CURSOR_SLOT_DRAG_THRESHOLD = 5;

/**
 * Stacking offset factor (0.05 = 5% of object size)
 */
export const STACKING_OFFSET_FACTOR = 0.05;

/**
 * Pool panel display constants
 */
export const POOL_PANEL_DEBUG_INFO_SIZE = 'text-xs';
export const POOL_PANEL_DEBUG_BG = 'bg-red-900 bg-opacity-90';
export const POOL_PANEL_ZONE_INFO_BG = 'bg-slate-800 bg-opacity-90';